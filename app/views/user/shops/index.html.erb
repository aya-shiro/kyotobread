<div class="container mt-4">
  <div class="col-md-10 mx-auto">
    <h5>ショップ一覧</h5>

    <% @shops.each do |shop| %>
      <div class="row mt-3 border p-3">
        <div class="col-md-1 border py-2"style="color: black;">
          <%= link_to shop_path(shop.id) do %>
          <h1 class=""><i class="fa-solid fa-store"></i></h1>
          <% end %>
        </div>
        <div class="col-md-6 mt-4">
          <%= link_to shop_path(shop.id) do %>
            <h5 style="color: black;"><%= shop.shop_name %><h5>
            <!--<p style="color: black;">公式リンク：<%#= shop.online_url %></p><br>-->
          <% end %>
        </div>
      </div>
    <% end %>

  </div>
</div>





















<div class="container mt-4">
  <div class="col-md-10 mx-auto">
    <h5>ショップ一覧</h5>

    <table class="table">
      <tr>
        <td>店名</td>
        <td>公式リンク</td>
        <td>オンラインショップ</td>
      </tr>

      <% @shops.each do |shop| %>
          <tr>
            <td>
              <%= link_to shop_path(shop.id) do %>
                <%= shop.shop_name %>
              <% end %>
            </td>
            <td><%= shop.online_url %></td>
            <td><%= shop.shop_url %></td>
          </tr>
      <% end %>

    </table>

  </div>
</div>