<h1>User::Users#show</h1>
<p>Find me in app/views/user/users/show.html.erb</p>

<h3>マイページ</h3>
<%= current_user.get_user_image(100, 100) %>
<%= current_user.name %>
<%= current_user.user_info %>
<%= link_to "プロフィールの編集", users_mypage_edit_path(current_user.id) %>

<h3>過去の投稿</h3>

<% if @breads.exists? %>
  <% @breads.each do |bread| %>
    <%= bread.get_bread_image(50, 50) %>
    <%= bread.name %>
  <% end %>
<% else %>
  <p>投稿はありません</p>
<% end %>