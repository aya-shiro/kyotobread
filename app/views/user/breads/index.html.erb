<div class="container mt-4">
  <div class="row mx-auto">

  <h4><%= @user.name %>さんの投稿一覧<h4>

  <!--以下をhomes/top.htmlと部分テンプレートを作る？-->

      <div class="col-md-8">
        <table>
          <tr>
            <%# @breads.each_slice(2) do |bread, row| %>  <!--この記述は何をしているのか？-->
            <% @breads.each do |bread| %>
              <td>
                <%= link_to bread_path(bread.id) do %>
                  <%= image_tag bread.get_bread_image(200, 200) %>
                <% end %>
                <%= bread.bread_name %>
              </td>
            <% end %>
          </tr>
        </table>
      </div>

  </div>
</div>